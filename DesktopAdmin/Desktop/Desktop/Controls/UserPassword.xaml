<UserControl x:Class="Desktop.Controls.UserPassword"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Desktop.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid>
            <TextBox Name="TextText" TextChanged="TextText_TextChanged" Visibility="Collapsed"></TextBox>
            <PasswordBox Name="PasswordText"></PasswordBox>
        </Grid>
        <Button Name="BtnSee" Click="BtnSee_Click" Grid.Column="1">👁️‍🗨️</Button>
    </Grid>
</UserControl>
